module 0x1::expressions {
    fun main() {
        if (true
                && false
        ) {};

        if (
        true
        && false
        ) {};

        vector[
        1,
        1,
        ]
    }
}
