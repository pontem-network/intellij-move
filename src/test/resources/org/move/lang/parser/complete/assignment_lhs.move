script {
    fun main() {
        let a = 1;
        let (a, b) = (1, 2);
        *a = 3;
        let R { a, b } = get_record();

        record.val = 1;
        record.val = vector<vector<vector<u8>>>;
        (record.val = 1);
    }
}