module M {
    fun main() {
        (1 + 1) * (1 + 1);
        (!!true + !!true) * !!false;
        1 % 2;
        1 ^ 2;

        *a = 1 + 2 * (3 + 5) * 4;

        a < b && b > 2;

        v1[1+1];
    }
}
