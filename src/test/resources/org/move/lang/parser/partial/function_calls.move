script {
    fun main() {
        call(1;
        call(1 +);
        call(1, 2 +);
        call(1, 2,,);
        call(1, 2,,4);
        assert()

        foo4(1,;
        foo5(;

        foo6(1,
        let _ = 0;

        foo7(1, assert;

        let a = call(,);
        call(& );
        call(&mut );

        call10(,,,);

        assert!(s.val +);
        assert!(s.val !=);
        assert!(s.val ==);

        spec {
            call(a,
        }
    }
}
